# Model Configuration
MODEL_PATH=models/waste_classifier.pt
VIT_MODEL_NAME=vit_base_patch16_224
USE_CUDA=auto

# API Configuration
API_PREFIX=/api
MAX_UPLOAD_SIZE=10485760

# Database Configuration (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=waste_segregation

# For development without MySQL, the system will run with in-memory storage
